import React from 'react';
const basket = require('../../assets/basket.png')

const Basket = () => {
    return (
        <div>
            <img src={basket} alt="" />
        </div>
    );
};

export default Basket;